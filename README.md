# Shift-Summarizer-Notifier
This template automatically determines the next worker on the schedule for any given shift. It automatically compiles the previous shift's notes into a concise summary and sends it directly to the next worker.
